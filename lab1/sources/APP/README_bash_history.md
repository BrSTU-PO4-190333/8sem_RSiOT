```bash
# NestJS
npx @nestjs/cli new app
npx @nestjs/cli g res users
npx @nestjs/cli g res posts
npx @nestjs/cli g res postCommmets

# npm packages
npm i -D @nestjs/cli     # nest
npm i @nestjs/config     # env
npm i cross-env          # env
npm i @nestjs/swagger    # swagger
npm i swagger-ui-express # swagger
npm i @nestjs/typeorm    # typeorm
npm i typeorm            # typeorm
npm i pg                 # postgres
npm i reflect-metadata   # postgres
npm i class-validator    # dto
npm i class-transformer  # dto
npm i bcrypt             # hash
npm i @types/bcrypt      # hash
#npm i uuid
#npm i @google-cloud/pubsub

# change npm to yarn
yarn add -D @nestjs/cli     # nest
yarn add @nestjs/config     # env
yarn add cross-env          # env
yarn add @nestjs/swagger    # swagger
yarn add swagger-ui-express # swagger
yarn add @nestjs/typeorm    # typeorm
yarn add typeorm            # typeorm
yarn add pg                 # postgres
yarn add reflect-metadata   # postgres
yarn add class-validator    # dto
yarn add class-transformer  # dto
yarn add bcrypt             # hash
yarn add @types/bcrypt      # hash
#yarn add uuid
#yarn add @google-cloud/pubsub
```
